export interface HistoryIdm {
    tahun: string;
    skor: number;
}

export interface IndikatorIdm {
    no: number;
    indikator: string;
    skor: number;
    keterangan: string;
    kegiatan: string;
}
